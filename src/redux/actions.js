export const setImageData = (imageData) => ({
    type: 'SET_IMAGE_DATA',
    payload: imageData,
  });
  
  export const setImageScale = (scale) => ({
    type: 'SET_IMAGE_SCALE',
    payload: scale,
  });
  
  export const setImagePosition = (position) => ({
    type: 'SET_IMAGE_POSITION',
    payload: position,
  });
  